{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet build -c Release && dotnet publish -c Release -o /app/publish"
  },
  "deploy": {
    "startCommand": "dotnet /app/publish/Backend.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

